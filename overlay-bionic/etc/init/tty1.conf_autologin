# tty1 - getty
#
# This service logs in the user automatically.

start on stopped rc RUNLEVEL=[2345]
stop on runlevel [!2345]

respawn

# Log in automatically.
exec /bin/login -f OPENFRAMEUSER < /dev/tty1 > /dev/tty1 2>&1
